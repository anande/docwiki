{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to My Documentation","text":"<p>This is a sample documentation site built with MkDocs and the Material theme.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Fast and responsive design</li> <li>Full-text search</li> <li>Dark/light mode toggle</li> <li>Syntax highlighted code blocks</li> <li>GitHub Pages deployment ready</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>def hello_world():\n    \"\"\"A simple hello world function.\"\"\"\n    print(\"Hello, World!\")\n\nhello_world()\n</code></pre>"},{"location":"#admonitions","title":"Admonitions","text":"<p>Pro Tip</p> <p>You can use admonitions to highlight important information.</p> <p>Warning</p> <p>This is a warning block.</p> <p>Did you know?</p> <p>MkDocs Material supports tabbed content, diagrams, and much more.</p>"},{"location":"#tabbed-content","title":"Tabbed Content","text":"PythonJavaScriptBash <pre><code>print(\"Hello from Python!\")\n</code></pre> <pre><code>console.log(\"Hello from JavaScript!\");\n</code></pre> <pre><code>echo \"Hello from Bash!\"\n</code></pre>"},{"location":"#table-example","title":"Table Example","text":"Feature Supported Search \u2705 Dark Mode \u2705 GitHub Pages \u2705 Code Copy \u2705"},{"location":"about/","title":"About","text":"<p>This documentation site is built with:</p> <ul> <li>MkDocs \u2014 static site generator</li> <li>Material for MkDocs \u2014 theme by squidfunk</li> <li>GitHub Pages \u2014 hosting</li> </ul>"},{"location":"about/#license","title":"License","text":"<p>See the LICENSE file for details.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.8+</li> <li>pip</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>Activate the virtual environment and install dependencies:</p> <pre><code>source venv/bin/activate\npip install mkdocs-material\n</code></pre>"},{"location":"getting-started/#local-development","title":"Local Development","text":"<p>Run the local dev server:</p> <pre><code>mkdocs serve\n</code></pre> <p>Then open http://127.0.0.1:8000 in your browser.</p>"},{"location":"getting-started/#deploy-to-github-pages","title":"Deploy to GitHub Pages","text":"<pre><code>mkdocs gh-deploy\n</code></pre> <p>This builds the site and pushes it to the <code>gh-pages</code> branch automatically.</p> <p>GitHub Pages Setup</p> <p>Make sure GitHub Pages is configured to serve from the <code>gh-pages</code> branch in your repo settings under Settings \u2192 Pages.</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/","title":"K8s","text":""},{"location":"how_kubespray_k3d_generate_kubeconfig/#how-does-kubespray-and-k3d-generate-kubeconfig-on-installing-the-k8s-clusters-give-detailed-info-on-same-make-it-markdown-and-squid-funk-mkdocs-friendly-to-directly-paste-it-in-the-md-file","title":"how does kubespray and k3d generate kubeconfig on installing the k8s clusters. give detailed info on same. Make it markdown and squid-funk-mkdocs friendly to directly paste it in the .md file","text":"<p>Below is a detailed, MkDocs/Material\u2011friendly explanation you can paste directly into a <code>.md</code> file.</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#how-kubeconfig-is-generated-by-kubespray-and-k3d","title":"How kubeconfig Is Generated by Kubespray and k3d","text":"<p><code>kubeconfig</code> files tell <code>kubectl</code> (and other clients) how to reach a cluster and how to authenticate. By default, <code>kubectl</code> looks for a file named <code>config</code> in <code>~/.kube/</code>, but tools like Kubespray and k3d generate and manage additional kubeconfigs for the clusters they create.^1^3</p> <p>This section explains, in detail, how kubeconfig generation works in Kubespray (Ansible\u2011based bare\u2011metal/VM clusters) and k3d (k3s in Docker).</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#common-kubeconfig-concepts","title":"Common kubeconfig concepts","text":"<ul> <li>A kubeconfig file is a YAML document containing:<ul> <li><code>clusters</code>: API server endpoints and TLS settings.</li> <li><code>users</code>: credentials (client certificates, tokens, etc.).</li> <li><code>contexts</code>: a tuple of <code>(cluster, user, namespace)</code> to make a \"profile\".</li> </ul> </li> <li><code>kubectl</code> chooses which cluster to talk to based on the current context.</li> <li>You can point <code>kubectl</code> to a specific file by:<ul> <li>Setting <code>KUBECONFIG=/path/to/kubeconfig</code>.</li> <li>Using <code>kubectl --kubeconfig=/path/to/kubeconfig ...</code>.^4^5</li> </ul> </li> </ul>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#kubespray-kubeconfig-generation-flow","title":"Kubespray: kubeconfig generation flow","text":"<p>Kubespray is an Ansible playbook collection that installs a production\u2011grade Kubernetes cluster on your own hosts. As part of the control\u2011plane bootstrap, it generates kubeconfig files on the master/control\u2011plane nodes and optionally on worker nodes.^6^8</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#where-kubeconfig-lives-with-kubespray","title":"Where kubeconfig lives with Kubespray","text":"<p>On each control\u2011plane node, Kubespray (via <code>kubeadm</code>) creates:</p> <ul> <li><code>/etc/kubernetes/admin.conf</code> \u2013 admin kubeconfig (cluster\u2011admin privileges).</li> <li><code>/etc/kubernetes/kubelet.conf</code> \u2013 kubelet client config.</li> <li><code>/etc/kubernetes/controller-manager.conf</code>, <code>scheduler.conf</code>, etc.</li> </ul> <p>Kubespray\u2019s documentation calls out that the kubeconfig files it generates generally point to <code>localhost</code> on control\u2011plane nodes, or to a local nginx proxy / external load balancer if configured.^8</p> <p>\u201cThe kubeconfig files generated will point to localhost (on kube_control_planes) and kube_node hosts will connect either to a localhost nginx proxy or to a loadbalancer if configured.\u201d^6</p> <p>Practically:</p> <ul> <li>Control\u2011plane nodes talk to the API server via <code>https://127.0.0.1:6443</code> (or a local proxy).</li> <li>Worker nodes may have kubeconfigs that point at:<ul> <li>A local nginx proxy on the node, or</li> <li>A dedicated external load balancer fronting multiple API servers.^8</li> </ul> </li> </ul>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#how-kubespray-generates-kubeconfig","title":"How Kubespray generates kubeconfig","text":"<p>The rough flow (simplified) is:</p> <ol> <li>Kubespray runs <code>kubeadm init</code> on the first control\u2011plane node <code>kubeadm</code>:<ul> <li>Generates a CA and server/client certificates.</li> <li>Writes <code>admin.conf</code> and other component configs under <code>/etc/kubernetes/</code>.^9</li> </ul> </li> <li>Ansible templates and copies kubeconfig Kubespray\u2019s Ansible roles:<ul> <li>Read <code>admin.conf</code> from <code>/etc/kubernetes/admin.conf</code>.</li> <li>Template or copy kubeconfig files to the appropriate locations on control\u2011plane and worker nodes.</li> <li>Adjust the <code>server:</code> field to point to either:<ul> <li><code>https://127.0.0.1:6443</code> (local API), or</li> <li>An HA load balancer fronting the API servers.^6</li> </ul> </li> </ul> </li> <li>Optional: expose kubeconfig to the operator machine Common patterns (not automatic unless you script it):<ul> <li>Copy <code>/etc/kubernetes/admin.conf</code> from a control\u2011plane node to your workstation:</li> </ul> </li> </ol> <pre><code>scp ubuntu@cp-1:/etc/kubernetes/admin.conf ~/kubeconfigs/genestack-admin.conf\nexport KUBECONFIG=~/kubeconfigs/genestack-admin.conf\nkubectl get nodes\n</code></pre> <pre><code>- Merge it into your default `~/.kube/config` using `kubectl config` commands.[^2][^4]\n</code></pre> <ol> <li>Kubespray and \u201clocalhost kubeconfig\u201d Since the kubeconfigs generated point at <code>localhost</code> on the control\u2011plane nodes, when you SSH to a control\u2011plane node and run:</li> </ol> <pre><code>export KUBECONFIG=/etc/kubernetes/admin.conf\nkubectl get nodes\n</code></pre> <p>it will talk to the API server bound to <code>127.0.0.1:6443</code> on that host.^8</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#key-characteristics-of-kubespray-kubeconfig-generation","title":"Key characteristics of Kubespray kubeconfig generation","text":"<ul> <li>Source of truth is the <code>admin.conf</code> produced by <code>kubeadm</code> on the first control\u2011plane node.^9</li> <li>Transport is <code>https</code> with client certs (cluster\u2011admin credentials).</li> <li>Endpoints are typically <code>localhost</code> or a configured load balancer, not a public IP directly.^6</li> <li>Distribution to operators is an explicit step (copy the file off the node); Kubespray does not automatically write kubeconfig on your laptop.</li> </ul>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#k3d-kubeconfig-generation-flow","title":"k3d: kubeconfig generation flow","text":"<p>k3d runs k3s (a lightweight Kubernetes distribution) inside Docker containers. Unlike Kubespray, it has direct control over both the control\u2011plane and the CLI environment, so it can generate and update kubeconfigs on the fly.^10</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#where-kubeconfig-lives-with-k3d","title":"Where kubeconfig lives with k3d","text":"<p>By default, k3d integrates with your existing kubeconfig:</p> <ul> <li>It detects the \"default\" kubeconfig as either:<ul> <li>The file pointed to by <code>KUBECONFIG</code> (if it references exactly one file).</li> <li>Or <code>~/.kube/config</code> if <code>KUBECONFIG</code> is unset.^11</li> </ul> </li> </ul> <p>k3d can:</p> <ul> <li>Write a cluster\u2011specific kubeconfig under:<ul> <li><code>~/.k3d/kubeconfig-&lt;cluster-name&gt;.yaml</code>.^11</li> </ul> </li> <li>Merge cluster details into your default kubeconfig and optionally switch context.</li> </ul>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#how-k3d-generates-kubeconfig-during-cluster-creation","title":"How k3d generates kubeconfig during cluster creation","text":"<p>When you run:</p> <pre><code>k3d cluster create mycluster\n</code></pre> <p>the steps are roughly:</p> <ol> <li>k3s server inside Docker creates its own kubeconfig k3s writes a kubeconfig file (usually <code>/etc/rancher/k3s/k3s.yaml</code> inside the container), which points to the cluster\u2019s API endpoint and contains the token/certs.^12</li> <li>k3d reads that kubeconfig from inside the container<ul> <li>It inspects the k3s container.</li> <li>Reads the internal kubeconfig file.</li> <li>Adapts the <code>server:</code> URL to a host\u2011accessible endpoint (e.g., <code>https://0.0.0.0:&lt;mapped-port&gt;</code> or <code>https://k3d-mycluster-server-0:6443</code> as seen from your host).^11</li> </ul> </li> <li>k3d either writes or merges kubeconfig on the host</li> </ol> <p>By default (k3d v5.x+):^11     - <code>k3d cluster create mycluster --kubeconfig-update-default</code>         - Merges the new cluster into your default kubeconfig file (<code>~/.kube/config</code> or <code>$KUBECONFIG</code>).         - Does not change the current context unless you also pass <code>--kubeconfig-switch-context</code>.     - If you don\u2019t disable the default behavior:         - A <code>cluster</code>, <code>user</code>, and <code>context</code> entry named something like <code>k3d-mycluster</code> are added to your kubeconfig.         - You can list them with:</p> <pre><code>kubectl config get-contexts\n</code></pre> <p>k3d also supports:     - Writing a standalone kubeconfig file:</p> <pre><code>k3d kubeconfig write mycluster\n</code></pre> <p>This creates or updates <code>~/.k3d/kubeconfig-mycluster.yaml</code>.^11     - Printing a kubeconfig to stdout:</p> <pre><code>k3d kubeconfig get mycluster &gt; ./kubeconfig-mycluster.yaml\nexport KUBECONFIG=./kubeconfig-mycluster.yaml\nkubectl get nodes\n</code></pre> <p>^10</p> <ul> <li>Merging into another file after creation:</li> </ul> <pre><code>k3d kubeconfig merge mycluster --output some/other/file.yaml\n# or into default kubeconfig:\nk3d kubeconfig merge mycluster --kubeconfig-merge-default\n</code></pre> <p>Add <code>--kubeconfig-switch-context</code> if you want the new cluster to become current.^10</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#example-flows-with-k3d","title":"Example flows with k3d","text":"<p>1. Default behavior (merge into <code>~/.kube/config</code>)</p> <pre><code>k3d cluster create mycluster\nkubectl config get-contexts          # shows context k3d-mycluster\nkubectl config use-context k3d-mycluster\nkubectl get nodes\n</code></pre> <p>Here, k3d:</p> <ul> <li>Pulls the kubeconfig from k3s inside Docker.</li> <li>Merges it into your default kubeconfig.</li> <li>Leaves current context unchanged unless <code>--kubeconfig-switch-context</code> is used.^11</li> </ul> <p>2. Standalone kubeconfig only</p> <pre><code>k3d cluster create mycluster --kubeconfig-update-default=false\nk3d kubeconfig write mycluster       # creates ~/.k3d/kubeconfig-mycluster.yaml\nexport KUBECONFIG=~/.k3d/kubeconfig-mycluster.yaml\nkubectl get nodes\n</code></pre> <p>This isolates the cluster config in a dedicated file, which is often useful for scripting or demos.^10</p>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#key-characteristics-of-k3d-kubeconfig-generation","title":"Key characteristics of k3d kubeconfig generation","text":"<ul> <li>Automatic: k3d always has a kubeconfig for each cluster and can either:<ul> <li>Merge it into your main kubeconfig, or</li> <li>Write it separately under <code>~/.k3d/</code>.^11</li> </ul> </li> <li>Context management:<ul> <li>Creation can update default kubeconfig and optionally switch context via <code>--kubeconfig-switch-context</code>.^10</li> </ul> </li> <li>Host\u2011visible endpoint:<ul> <li>It rewrites the <code>server:</code> URL from the k3s internal one to a host\u2011reachable address/port.</li> </ul> </li> </ul>"},{"location":"how_kubespray_k3d_generate_kubeconfig/#summary-kubespray-vs-k3d-kubeconfig-behavior","title":"Summary: Kubespray vs k3d kubeconfig behavior","text":"Aspect Kubespray k3d Primary environment Bare\u2011metal/VM hosts, often in clouds or on\u2011prem k3s inside Docker on local/development machines Where kubeconfig is created <code>/etc/kubernetes/admin.conf</code> on control\u2011plane nodes via <code>kubeadm</code> Inside k3s server container, then exported to host Default kubeconfig endpoint <code>https://127.0.0.1:6443</code> or local nginx / LB on the nodes Host\u2011reachable API endpoint (mapped Docker port) How operator gets kubeconfig Manual copy from node, then <code>KUBECONFIG</code> or merge into <code>~/.kube/config</code> Automatically merged into or written alongside your default kubeconfig Integration with <code>~/.kube/config</code> Not automatic; you decide how/where to merge Automatic (unless disabled), with options to merge or write standalone files Context switching Manual via <code>kubectl config use-context</code> Optional automatic switch via <code>--kubeconfig-switch-context</code> during merge/write <p>Both tools ultimately produce standard kubeconfig files; the main differences are where the files originate and how much automation they provide for placing and merging them into your local configuration.^6</p> <p>Do you want a follow\u2011up section with ready\u2011to\u2011copy shell snippets for \u201cKubespray cluster: pull admin.conf and merge into my ~/.kube/config\u201d and \u201ck3d cluster: isolate kubeconfig per project\u201d? ^13^15</p> \u2042"}]}